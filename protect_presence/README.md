# UniFi Protect Presence

Protect → MQTT bridge for recognized-face smart-detect events + USL-Environmental
leak state (private-API, read from `event.raw` via `subscribe_websocket`).

See [DOCS.md](DOCS.md) for configuration and the MQTT contract.
